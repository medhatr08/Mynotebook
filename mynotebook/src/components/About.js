
import React from "react";
//import noteContext from "../context/notes/noteContext";
function About() {
    // const a=useContext(noteContext)
    // useEffect(()=>{
    //     a.update()
    // },[])
    return (
      <div>
       {/* This is About {a.state.name} and in class in {a.state.class} */}
       This is About page
      </div>
    );
  }
  export default About